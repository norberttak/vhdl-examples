# Makefile automatically generated by ghdl
# Version: GHDL 0.37 (Ubuntu 0.37+dfsg-1ubuntu1) [Dunoon edition] - mcode code generator
# Command used to generate this makefile:
# /usr/bin/ghdl-mcode --gen-makefile multiplexer

GHDL=/usr/bin/ghdl-mcode
GHDLFLAGS= -v --std=08 -fexplicit

.PHONY: all

# Default target : elaborate
all : init elab run

# Targets to analyze libraries
init:
	$(GHDL) -a $(GHDLFLAGS) ../common/utest.vhdl
	$(GHDL) -a $(GHDLFLAGS) multiplexer.vhdl
	$(GHDL) -a $(GHDLFLAGS) multiplexer-tb.vhdl

# Elaborate target
elab:
	$(GHDL) -e $(GHDLFLAGS) multiplexer_tb

# Run target
run:
	$(GHDL) -r $(GHDLFLAGS) multiplexer_tb --wave=wave.ghw

clean:
	rm -f *.cf
	rm -f *.o
